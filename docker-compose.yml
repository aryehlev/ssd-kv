version: '3.8'

services:
  ssd-kv:
    build: .
    ports:
      - "7777:7777"   # RESP (Redis-compatible) protocol
    volumes:
      - ssd-kv-data:/data
    environment:
      - RUST_LOG=info
    command: ["ssd-kv", "--data-dir", "/data", "--bind", "0.0.0.0:7777"]

volumes:
  ssd-kv-data:
